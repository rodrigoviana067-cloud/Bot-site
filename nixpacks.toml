[phases.setup]
nixPkgs = ['nodejs', 'python311', 'supervisor']

[phases.build]
cmds = [
    'npm install',
    'python3 -m venv venv && venv/bin/pip install -r requirements.txt'
]

[start]
cmd = 'bash start.sh'
